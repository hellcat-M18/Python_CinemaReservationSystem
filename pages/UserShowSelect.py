import rich
from rich.console import Console

console = Console()


def run(session: dict) -> dict:
    """上映回選択（WIP）"""
    console.print("[bold]UserShowSelect[/bold] (WIP)")
    # TODO: movie_idからshows一覧→show選択→座席選択へ
    return session
