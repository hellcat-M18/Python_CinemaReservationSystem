import rich
from rich.console import Console

console = Console()


def run(session: dict) -> dict:
    """座席選択（WIP）"""
    console.print("[bold]UserSeatSelect[/bold] (WIP)")
    # TODO: hallレイアウト読み込み→予約済み座席を埋める→座席入力
    return session
