import rich
from rich.console import Console

console = Console()


def run(session: dict) -> dict:
    """映画一覧（ユーザー向け）（WIP）"""
    console.print("[bold]UserMovieBrowse[/bold] (WIP)")
    # TODO: movies一覧→選択→上映回選択へ
    return session
